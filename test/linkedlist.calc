package main

import (
    "github.com/Chronostasys/calc/runtime/linkedlist"
)


func testLinkedList() void {
    list := linkedlist.New<int>()
    list.Push(1)
    list.Push(2)
    list.Push(3)
    printIntln(list[2])
    printIntln(list.Len())
    printIntln(list.Shift())
    printIntln(list.Pop())
    printIntln(list.Len())
    return
}
